# pi-popchat 产品需求文档

版本：0.3 · 日期：2026-09-11 · 状态：一期功能已实现；2026-09-11 确认的过程展示、Pi 标题同步与划词入口为待实现需求，不代表已通过验收。已有交付结果及操作系统验证边界见 delivery-report.md。

本文中的“必须”为已经确认的产品需求。“兼容性约束”为 Pi 0.84.1 实测结果对实现的限制。“待定”不属于已确认决策，不应在实现时悄然固化。

## 1. 产品定位与边界

pi-popchat 是面向 macOS 的桌面 Agent 客户端。用户通过全局快捷键进入临时浮窗交流，也可以在正式主窗口中查看、管理和继续历史会话。

产品价值是桌面交互体验、会话管理和成熟 Agent 的接入，不按具体业务场景设计工作流，不重写 Agent 的规划、工具执行或模型调用能力。

关系为 **应用 → Agent → 模型**。一期仅支持 Pi；架构需为后续接入 Codex、Claude Code 保留边界，但不在一期实现这些接入，也不提前承诺所有 Agent 能力完全一致。

| 范围 | 一期要求 |
|---|---|
| 平台 | 仅 macOS；最低系统版本及 Intel 支持待技术选型决定 |
| Agent | Pi；应用自动管理其后台进程，日常使用无需打开终端或手动启动 Pi |
| 安装与配置 | 不安装 Agent；检测缺失或配置不完整时提供安装/配置指引及重新检测入口 |
| 模型与认证 | 不直接对接模型、不管理凭证；模型列表和切换只使用 Agent 暴露的能力 |
| 现有资源 | 复用用户 Pi 配置、认证、模型、扩展和 Skills；受 RPC 兼容性限制 |
| 会话范围 | 只管理应用自身创建的会话，不导入或接管终端会话 |

## 2. 窗口与应用生命周期

### 2.1 浮窗

| 操作或状态 | 必须表现 |
|---|---|
| 全局快捷键 | 默认 `⌥Space`，可修改；冲突时提示重新设置 |
| 唤起 | 打开可输入的 Agent 对话浮窗，无需先打开主窗口或终端 |
| 置顶 | 浮窗保持在其他普通窗口上方 |
| 按 Esc | 收起浮窗，不停止或取消 Agent 执行 |
| 点击浮窗外部 | 只转移焦点，不隐藏浮窗，也不影响执行 |
| 停止任务 | 使用独立停止操作，不与隐藏窗口绑定 |
| 转入主窗口 | “在主窗口中打开”定位同一会话并收起浮窗，消息与执行连续 |

快捷键唤起以按下时鼠标所在屏幕为准，并在该屏可用区域居中，不在指针附近定位。浮窗已在别屏可见时移动过来并聚焦输入框，同屏再次按快捷键时收起。唤起后移动鼠标不改变浮窗位置，应用激活过程沿用本次唤起时选定的屏幕。鼠标位于菜单栏或 Dock 区域时仍归属该屏；选屏失败时回退到系统主窗口屏幕，再回退首屏。该规则于 2026-09-10 确认，替代 2026-09-09 的快捷键按活动窗口选屏规则。

主窗口初始 1080×760，浮窗 520×620；快捷键每次唤起按鼠标选屏并居中，不持久记忆浮窗坐标。应用顶部“窗口 → 打开浮窗”仍沿用活动窗口选屏，仅打开、不切换收起；右上角状态栏图标菜单只提供打开主窗口和退出。已验证本应用全屏 Space 中浮窗可见并获得输入焦点，不把“置顶”扩大解释为覆盖所有系统界面。

### 2.2 主窗口与驻留

关闭主窗口或浮窗不等于退出应用。应用继续在菜单栏驻留，全局快捷键及后台任务继续有效。

用户主动选择“退出”才真正退出。仍有任务执行时先提示，允许取消退出或继续退出。退出后保留已落盘历史；不承诺中断的工具执行能从原位置自动恢复。

应用或 Pi 异常退出后，保留已落盘历史，将受影响任务标记为“已中断”。重启时不自动重发此前正在执行或排队的消息，由用户决定继续或重试，避免重复执行有副作用的操作。

### 2.3 浮窗会话超时规则

“临时”指入口和窗口形态，不代表会话不保存。

| 再次唤起时的情况 | 默认打开 |
|---|---|
| 尚无浮窗会话 | 新会话 |
| 距最近一次隐藏未超过 30 分钟 | 原浮窗会话，包括草稿和已有状态 |
| 已超过 30 分钟，原会话未执行且未等待回答 | 新会话 |
| 原会话仍在执行或等待用户回答 | 原会话，即使超过 30 分钟 |

计时起点为最近一次隐藏浮窗。点击外部不会隐藏，因此不会启动这次计时。阈值一期默认 30 分钟；是否提供用户配置入口待定。

超时新建不删除、不关闭旧会话，旧会话可以随时从主窗口找到并继续。这里的新建只改变浮窗当前会话。

2026-09-10 确认：首次打开、手动新建和超时换新均进入全应用共享的一份未发送草稿，不创建正式历史会话。草稿文字、附件和目录选择持久保存，切换历史或重启后可通过“新对话”恢复；再次新建或超时复用已有草稿。主窗口和浮窗同时显示草稿时共享同一份内容，浏览历史仍各自独立。

首次有效提交（文字、仅附件或 `/` 命令）成功保存本地消息后，草稿原子转为正式会话，并进入历史；双窗口引用同一 ID，不创建副本。Agent 启动或执行失败保留消息和错误，本地保存失败保留草稿、不创建历史，也不投递。首次发送前允许启动 Agent 查询模型、命令等元数据，此过程不产生应用历史记录。旧空会话不自动清理，由用户手动删除。

浮窗和主窗口分别记住自己的当前会话。主窗口浏览或切换历史不会自动改变浮窗会话。二者若展示同一会话，应看到同一份消息和执行状态，不能创建副本。

## 3. 对话输入、展示与运行

### 3.1 输入

一期必须支持文字、粘贴图片和拖入文件。附件在发送前可辨识，不能将“接收附件”暗示为所有 Agent 都能理解所有格式。

输入框支持 `/` 菜单，展示、搜索并调用 Agent 暴露的 Skills、提示词模板、扩展命令。应用不维护另一套业务命令体系。Agent 未暴露的终端内置命令不能假装可执行。

提供 Agent 支持的模型选择与切换；候选列表和执行结果均来自 Agent，客户端不自行请求模型服务。

语音输入本期不做，后续单独评估。

### 3.2 输出和必要交互

| 输出类别 | 展示原则 |
|---|---|
| Agent 回复 | 流式展示，支持回复所需的常规文本格式和可打开的文件链接 |
| 执行过程 | 简短过程记录，展示动作与进行中、完成、失败状态；具体规则见下文 |
| 原始 thinking | 不展示 |
| 工具参数、工具输出和执行日志 | 不展示完整参数、原始输出或日志；允许展开查看动作对应的具体对象及其状态 |
| 明确的选择、确认、输入请求 | 展示可操作的问题，并将用户结果交回 Agent |
| 失败或停止 | 明确表达状态，不能显示为任务成功完成 |

不增加模型调用来猜测执行进度、总结工具日志或判断回复是否在提问。普通文本提问直接作为回复展示，只有明确交互请求进入“等待回答”状态。

2026-09-11 确认以下规则，替代原先只展示“正在处理”等简短状态、完全不提供展开的要求：

- 执行时按顺序显示简短过程记录。默认只显示动作，例如“读取文件”；点击展开后查看具体对象，例如文件名，以及各对象的执行状态。展示内容来自 Agent 实际事件，不额外调用模型生成步骤描述。
- 只合并连续的同类动作。例如读取 A、读取 B 合为“读取文件 · 2 项”；随后搜索、再读取 C 时分别新增记录，不将 C 合回早先的读取组，保留执行顺序。
- 本轮结束后折叠为一行过程摘要，点击可展开；记录持久保留，重新打开历史仍可查看。最终失败时展开并露出失败步骤；停止、中断不得显示为成功完成，不能把某次工具失败直接等同于整轮最终失败。
- 无工具动作、尚未收到正文时显示“等待 Agent 响应 · 18 秒”等状态与耗时；收到正文后显示“正在生成回答”并流式展示。后续状态随实际事件变化，没有事件时不猜测 Agent 在做什么，不展示原始思考或虚构进度百分比。

### 3.3 并发与执行中的新消息

允许多个会话同时执行。用户切换或隐藏窗口不会中止任何任务。主窗口标识执行中和等待回答的会话。

执行中发送的新消息默认进入待发送队列。用户可对待发送消息选择“立即插入”，客户端需说明实际接收时机是当前步骤结束后，不能承诺强行抢占正在执行的工具。

兼容性约束：Pi 0.84.1 有 `follow_up` 和 `steer`，但 RPC 没有撤回单条消息或原子转换队列类型的接口。实现应由应用保留尚未提交的队列：默认等待当前执行结束后提交，插入时移出本地队列并使用 `prompt` 的 `streamingBehavior: "steer"`。这属于交互消息调度，不是重新实现 Agent 循环。

应用使用稳定消息 ID 去重；草稿及附件带版本做冲突检测，所有会话操作绑定发起时的会话 ID。队列持久化后才投递，重启后暂停；投递不确定时明确标识，不自动重发。不得静默丢失或重复发送消息。

### 3.4 划词工具条与自定义按钮（2026-09-11 确认，待实现）

用户在其他应用选中文字后，默认在选区附近自动出现操作工具条；此时不创建会话、不发送内容。提供上级设置“启用划词工具条”，默认开启；关闭时保留已有按钮配置。按钮全部删除时不显示工具条，设置页提供添加按钮和恢复默认按钮入口。

点击按钮后，每次创建独立新会话并立即发送按模板生成的请求，后续追问沿用该会话。保留原会话及未发送草稿，已有后台任务继续执行。调用动作绑定本次新会话 ID，并沿用保存后投递、失败保留、不自动重发等可靠性规则。

回答使用现有对话浮窗，复用现有选屏、居中定位、隐藏、聚焦及会话交互规则，不另设选区附近的回答窗口。工具条附近定位仅适用于工具条本身。点击动作必须打开并定位本次新会话，不能因浮窗已显示而触发快捷键的再次按下收起行为。

按钮由名称和提示词模板组成，允许用户自定义、修改和删除。“翻译”“解释”仅为初始按钮，不是不可删除的内置功能。模板只获取本次选中文字，不自动读取周边段落、整页内容或拼接原会话上下文。

默认“解释”使用系统首选语言。默认“翻译”将其他语言译为系统首选语言；选中文字已是系统首选语言时默认译为英语。系统首选语言本身为英语时，改用系统第二偏好语言；未设置第二偏好语言则在发送前让用户选择目标语言。默认按钮也使用模板，用户修改后以用户模板为准，不暗中补充翻译或解释指令。语言判断及默认翻译模板的实现方式需要验证，不将未经验证的能力归给 Pi。

首版模板变量如下，设置页支持点击插入：

| 变量 | 含义 | 示例 |
|---|---|---|
| `{{text}}` | 本次选中文字，保留原始换行 | `Hello world` |
| `{{language}}` | 系统首选语言，含地区信息 | `zh-CN` |
| `{{date}}` | 当前本地日期 | `2026-09-11` |
| `{{time}}` | 当前本地时间，24 小时制 | `14:30:05` |
| `{{timezone}}` | 系统时区 | `Asia/Shanghai` |

所有时间变量取点击按钮时的同一时刻。严格按模板替换，仅替换一遍；选中文字中的变量语法不递归展开。没有 `{{text}}` 时不自动附加选中文字。未知变量在保存时提示，但允许保存并原样发送。

技术验证边界：跨应用选中文字的可获取范围、系统权限、工具条焦点与选区保留、跨屏和全屏表现需在实现前验证并记录。不将“任意词”解释为所有应用或所有系统控件均保证可读取；2026-09-11 用户要求先完整实现再自行验证，不增加取词降级路径。

扩展验收结果见 [划词能力验收报告](selection-acceptance-2026-09-11.md)：原生三屏、可编辑网页及安全输入复测通过；只读网页、真实 Chrome/VS Code 焦点前置条件、全屏 Esc 和现有浮窗原生回归仍未通过。这些是实现前原型的验收记录；本轮已接入正式应用，跨应用实际效果由用户验证，不将原型结果当作正式功能验收通过。

## 4. 历史、目录与文件

### 4.1 历史管理

主窗口管理应用创建的所有会话，支持搜索、重命名、置顶、删除和继续对话。默认按最近活跃时间排序，置顶会话有明确展示位置。

2026-09-11 确认：会话标题以 Pi 为唯一数据源。读取 Pi 的标题并跟随标题变化，应用不独立维护另一套可编辑标题，不主动生成 AI 标题，也不自动向 Pi 设置标题。允许保存可重建的显示/搜索缓存，但缓存不成为另一标题来源。

保留手动重命名入口，用户提交时调用 Pi 的设置标题能力；以 Pi 返回的成功结果和标题状态为准，失败时明确提示，不保留仅在应用生效的改名。Pi 未提供标题时，首条用户消息前 32 个字符仅作为显示兜底，不写回 Pi；Pi 提供标题后立即使用其标题。仅附件等没有文字的情况保留既有“附件对话”等显示兜底。已有应用本地标题如何迁移、历史未运行时如何读取 Pi 标题，属于待验证的实施事项，不自动将旧标题写入 Pi。

技术验证后确认：Pi 会话尚未落盘时禁用手动重命名，并说明需等待 Pi 保存会话；落盘后恢复入口。不能仅凭应用已保存首条用户消息或 Pi 改名 RPC 返回成功就视为标题可持久保留。

搜索标题和可见消息正文。删除须确认，执行中的会话必须先停止；删除默认移除应用历史索引并保留工作文件和产物。若使用应用默认管理的目录，确认框展示目录完整路径，提供“删除会话”和“删除会话及工作目录”，后者明确说明目录内所有文件将被删除、无法撤销；用户自行选择的目录始终保留。升级前未记录目录来源的旧会话无法仅凭路径确认所有权，因此也保留目录，不提供递归删除选项。仍被其他会话或草稿使用、或路径已被替换的目录不能自动删除。不提供应用内回收站。文件夹、标签、会话分支管理不进入一期。

### 4.2 工作目录

默认采用应用管理的工作目录，并提供选择已有文件夹的能力。每个会话与其工作目录的关系应持久记录，以便续聊使用正确位置。

默认每个会话使用应用管理的独立子目录；新会话可选择已有目录，开始发送消息后目录固定，续聊沿用原目录。选择目录不是权限隔离，也不能被描述成沙箱。

普通文件需要交给 Agent 的可访问路径；Agent 是否有能力处理格式由其工具和 Skills 决定。图片可以使用 Pi 的原生图片输入。

### 4.3 文件打开

对话里的有效本地文件链接使用 macOS 默认应用打开，同时提供“在 Finder 中显示”。不做内置文件编辑器。

不假设 Agent 会发送统一的产物列表。回复文件链接的解析、相对路径基准、附件持久化沿用现有规则；删除会话时产物保留规则见第 4.1 节。

## 5. 通知

浮窗隐藏期间，任务完成或需要用户回答时发送 macOS 通知。通知本身不自动弹出浮窗或抢焦点；用户点击通知时恢复对应会话。

多个会话并行时，通知必须定位到触发事件的会话，而非当前浮窗或主窗口随意选中的会话。失败显示“任务未完成”，异常退出显示“任务已中断”；主窗口正在聚焦查看对应会话时抑制重复通知。

Pi 的 `prompt.success` 仅表示接收；`agent_end` 之后可能继续自动重试；`agent_settled` 才表示执行停止自动继续。客户端必须结合错误和停止原因判断实际结果，不能把 settled 一律当成业务成功。

## 6. Pi 兼容性基线

基线为本机 **Pi 0.84.1**。已通过真实 Pi 进程、本地模拟模型服务完成 17 项协议断言，详见 [验证报告](pi-compatibility-report.md)。

基础 RPC 对话、图片、工具生命周期、选择/确认/输入请求、模型切换、资源命令、多进程会话隔离、重启续聊和重试/错误均已验证。普通文件使用路径交接，排队转插入采用客户端待发送队列。

Pi 的 TUI 自定义组件 `custom()` 无法在 RPC 中使用。复用用户扩展必须尊重这个边界，不承诺所有扩展自动兼容。真实模型服务、所有文件格式及全部第三方扩展不在此次测试覆盖内。

2026-09-11 本机 0.84.1 核查：RPC `get_state.sessionName` 可读取标题，`set_session_name` 可设置现成标题，`session_info_changed` 提供变更事件，扩展可调用 `pi.setSessionName()`。未查到内置 AI 自动标题生成机制；官方命名扩展示例为手动设置。随后真实 Pi 子进程及回环模拟模型验证共 23 项通过，包括标题更新、清除、重启和首次落盘边界；尚未实现产品标题同步端到端验收。详见 [技术验证报告](technical-validation-2026-09-11.md)。参考 [官方 RPC 文档](https://github.com/earendil-works/pi/blob/main/packages/coding-agent/docs/rpc.md)；在线 main 不替代本机版本基线。

## 7. 验收场景

| ID | 场景 | 验收结果 |
|---|---|---|
| A01 | 已配置 Pi，未打开终端，按唤起快捷键 | 可进入对话；应用自行启动所需 Pi 进程 |
| A02 | Pi 缺失或未配置 | 显示可执行的安装/配置引导；不代装、不提取凭证 |
| A03 | 修改全局快捷键或发生冲突 | 修改成功或收到明确冲突提示 |
| A04 | 执行中按 Esc | 浮窗隐藏，任务继续 |
| A05 | 点击浮窗外部 | 浮窗保持显示，任务继续 |
| A06 | 隐藏后在阈值内或阈值外唤起 | 按第 2.3 节规则恢复或新建；旧会话仍能继续 |
| A07 | 主窗口切换历史 | 不改变浮窗当前会话 |
| A08 | 浮窗转入主窗口 | 定位同一会话，收起浮窗，不打断任务 |
| A09 | 多会话同时执行 | 状态和消息互不混淆；切换不终止任务 |
| A10 | 执行中发送并插入消息 | 默认排队；主动插入在 Agent 可接受时机交付，无重复 |
| A11 | 粘贴图片、拖入文件、使用 `/` 菜单 | 图片/文件正确交接；菜单来自 Agent 暴露资源 |
| A12 | Agent 请求用户选择或输入 | 可在应用中完成；答案返回正确会话与请求 |
| A13 | Agent 执行工具 | 一期已验收简短状态；新版要求简短过程记录及对象展开，不出现完整原始参数、thinking 或工具日志，新版待验收 |
| A14 | 隐藏期间完成或等待回答 | 通知不抢焦点；点击定位正确会话 |
| A15 | 历史会话管理 | 可搜索、重命名、置顶、删除和续聊；不包含终端会话 |
| A16 | 选择目录后续聊，打开产物 | 使用关联目录；文件以默认应用打开或在 Finder 定位 |
| A17 | 关闭主窗口 | 菜单栏、快捷键和后台执行继续工作 |
| A18 | 有任务时主动退出 | 先提示；可取消或继续；已落盘历史保留 |
| A19 | Pi 自动重试或最终失败 | 不提前通知成功，不把接收成功或执行停止当成任务成功 |

## 8. 后续决策范围

### 2026-09-11 新增验收要求（全部待实现、待验收）

| ID | 场景 | 预期表现 |
|---|---|---|
| B01 | 连续读取 A/B，搜索后读取 C | A/B 连续合并，展开显示对象及状态；搜索和读取 C 保持独立顺序 |
| B02 | 等待响应、流式正文、结束、失败及重开历史 | 状态和耗时可见；正文流式；结束折叠，失败步骤可见；历史保留过程，停止/中断不误报成功 |
| B03 | Pi 提供/更新标题及用户手动改名 | 读取和更新均以 Pi 为准；改名调用 Pi，失败有提示；无标题才显示首条消息前 32 字，不自动写回或生成标题 |
| B04 | 开关关闭、按钮删空、恢复默认及修改模板 | 关闭或删空不弹工具条且保留适用配置；可恢复默认按钮；严格按用户模板发送 |
| B05 | 选中文字并点击按钮，原浮窗已有会话或草稿 | 点击前不发送；点击后新建并发送，复用现有浮窗定位，原草稿/会话/任务保留，追问沿用新会话 |
| B06 | 默认翻译与解释，系统语言及备用语言不同 | 按第 3.4 节语言规则处理；无备用语言时在发送前选择；用户修改模板后不暗加默认指令 |
| B07 | 日期/时间变量、未写 text、未知变量、选中文字含变量 | 时间取点击时同一时刻；不补充选中文字；未知变量提示但原样发送；替换不递归 |

### 既有交付与范围

已实现 Go + Wails v3 + React 正式工程、Pi RPC 适配、SQLite 会话索引及本机签名应用。测试与独立评审结果见 delivery-report.md；不将已实现等同于所有第三方环境都已验证。

已实现 Enter 发送、Shift+Enter 换行和组合输入保护。最低声明 macOS 15、Apple Silicon，本机实际验收为 macOS 15.6.1。特殊系统窗口、物理中文候选框和跨系统版本仍需专项验证；不承诺未经测量的内存或唤起耗时。

不再引入按行业/任务类型定制的工作流、Agent 自研、模型网关、凭证系统、终端会话接管、语音或文件编辑器。

### 回答中的图片展示（2026-09-11）

回答中的 Markdown 图片应直接显示，HTTP/HTTPS 图片由 WebView 加载且不发送来源页信息；会话工作目录内的 PNG/JPEG/GIF/WebP 图片通过类型化接口读取预览，按发起时的 sessionId 解析相对路径。支持这些格式的内嵌 base64 图片及已发送图片附件的缩略图。图片宽度不能撑破主窗口或浮窗，点击路径/网络图片打开原图，失败时展示说明、重试和原图入口。自动读取本地图片限制 16 MB，并拒绝越出会话目录的路径和符号链接；目录外文件仍保留显式打开入口。本条替代早期验收中“远程图片变文字、不发起请求”的产品行为，不改变禁用原始 HTML 和危险链接的要求。
